---
import { createCookieSupabase } from "../../lib/supabase";

if (Astro.request.method === "POST") {
  const supabase = createCookieSupabase(Astro.request, Astro.cookies);
  await supabase?.auth.signOut();
}
return Astro.redirect("/admin/login");
---
